name: build-test
on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop
jobs:
  make-build:
    name: Build project
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@master
        - run: cd src && make artpad
